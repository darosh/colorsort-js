{
  "name": "colorsort-analysis",
  "version": "0.0.0",
  "devDependencies": {
    "colorsort": "workspace:*",
    "colorsort-compute": "workspace:*",
    "colorsort-data-palettes": "workspace:*",
    "colorsort-data-sorted": "workspace:*",
    "json-stringify-pretty-compact": "^4.0.0"
  },
  "exports": {
    "types": "./src/index.ts",
    "import": "./dist/colorsort-analysis.js",
    "require": "./dist/colorsort-analysis.umd.js"
  },
  "private": true,
  "scripts": {
    "build": "deno run -A npm:vite build",
    "dev": "deno run -A npm:vite build --watch",
    "lint": "oxlint ./src --fix && oxfmt ./src",
    "test": "deno run -A npm:vitest run"
  },
  "type": "module"
}
