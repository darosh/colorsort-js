{
  "name": "colorsort-data-sorted",
  "version": "0.0.0",
  "devDependencies": {
    "colorsort-js": "workspace:*",
    "colorsort-compute": "workspace:*",
    "colorsort-data-palettes": "workspace:*",
    "json-stringify-pretty-compact": "^4.0.0"
  },
  "exports": {
    "./sorted.json": "./dist/sorted.json"
  },
  "private": true,
  "scripts": {
    "build": "deno run -A npm:vite build",
    "build:fast": "cross-env FAST=1 deno run -A npm:vite build",
    "dev": "deno run -A npm:vite build --watch",
    "lint": "oxlint ./src --fix && oxfmt ./src"
  },
  "type": "module"
}
